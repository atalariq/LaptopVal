services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: laptopval
      POSTGRES_PASSWORD: laptopval
      POSTGRES_DB: laptopval
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata:
